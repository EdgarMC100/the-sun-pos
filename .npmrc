# Required for AWS Amplify deployment with pnpm
# Using pnpm@10.33.4 (pinned in amplify.yml) which allows build scripts by default
# Newer pnpm versions (10.34+) block dependency builds with ERR_PNPM_IGNORED_BUILDS
auto-install-peers=true
